# =============================================================================
# Ads Marketplace Frontend Environment (Vite)
# =============================================================================
# Copy to frontend/.env

# -----------------------------------------------------------------------------
# API + TON Manifest
# -----------------------------------------------------------------------------
# Backend API base URL. In local dev you can keep /api if proxied.
VITE_API_URL=https://<backend-domain>/api
# Optional. If omitted, frontend derives: <VITE_API_URL>/tonconnect-manifest.json
VITE_TON_CONNECT_MANIFEST_URL=

# -----------------------------------------------------------------------------
# TON + Support
# -----------------------------------------------------------------------------
VITE_TON_NETWORK=testnet
# Optional support contact (t.me link).
VITE_TELEGRAM_SUPPORT_URL=https://t.me/<support-bot-or-channel>

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------
VITE_ENABLE_ANALYTICS=false
VITE_FEATURE_CHANNEL_ANALYTICS=true
VITE_FEATURE_TON_ESCROW=true
VITE_DEAL_CHAT_DELETE_TOPICS_ON_CLOSE=true

# -----------------------------------------------------------------------------
# Currency
# -----------------------------------------------------------------------------
# Optional. Defaults to TON when omitted.
VITE_SUPPORTED_CURRENCIES=TON
# Optional. If omitted, first value from VITE_SUPPORTED_CURRENCIES is used.
VITE_DEFAULT_CURRENCY=TON

# -----------------------------------------------------------------------------
# Optional UI Override
# -----------------------------------------------------------------------------
# VITE_FORCE_THEME=light
